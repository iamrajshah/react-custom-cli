import { defaultAction } from '../../actions/{{ properCase name }}.action';
import { DEFAULT_ACTION } from '../../constants/{{ properCase name }}.constant';

describe('{{ properCase name }} actions', () => {
  describe('Default Action', () => {
    it('has a type of DEFAULT_ACTION', () => {
      const expected = {
        type: DEFAULT_ACTION,
      };
      expect(defaultAction()).toEqual(expected);
    });
  });
});
